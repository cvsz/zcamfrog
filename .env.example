# Camfrog Multi-ID Manager reads no environment variables and needs no .env file.
# This file exists only so tooling that expects it stays quiet.
# Never commit real credentials, tokens, or secrets.
APP_ENV=production
